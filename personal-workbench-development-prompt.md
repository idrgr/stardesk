# 星枢 StarDesk：个人工作台完整开发提示词

本文面向完全依赖 AI 工具开发的个人用户。默认做一个单人使用、桌面优先、兼顾手机的中文 Web 工作台，采用深色未来感界面，先实现本地可用版本，再扩展账号、同步和 AI 能力。

使用方法：

1. 将本文保存到开发项目中，作为长期需求文档。
2. 能持续自主开发的工具：发送文末「启动开发提示词」，让它读取本文并按阶段推进。
3. 单轮能力或上下文较弱的工具：附上本文，每次只发送一个「分阶段提示词」。
4. 更换模型或开启新对话：使用「接手项目提示词」，并提供当前代码和项目交接文档。
5. 后续加功能：使用「新增模块提示词」，避免让新模型重新搭建整个项目。

以下「一」至「十四」是可以直接交给开发 AI 的完整主提示词。

---

## 一、你的角色与交付目标

你是一名兼具产品设计、交互设计、前端开发、数据建模和质量验证能力的工程师。请根据本需求，实际开发一个可运行、可持续使用、可继续扩展的个人工作台。

产品暂定名称：星枢 StarDesk。

产品定位：帮助个人把长期方向拆成近期计划，把计划落实为今日行动，再通过记录和复盘不断调整。

核心体验：

- 早上打开首页，快速决定今天最重要的事情。
- 白天随时新增任务、记录灵感、开始专注或登记活动。
- 晚上查看实际完成情况，留下简短记录。
- 每周回顾个人提升、职业规划、兴趣爱好和运动健身的进展。
- 以后需要管理旅行、生活事务、个人项目等内容时，可以平滑增加模块。

请真正完成页面、交互、数据保存和必要验证。最终交付应当是能长期使用的产品。

开发基本规则：

1. 开始前检查当前项目、已有代码、依赖和运行方式。存在可用项目时沿用它，不擅自推倒重建。
2. 普通实现选择由你根据本文自主决定。只有真实阻塞、破坏已有数据或需要额外付费服务时，才提出具体问题。
3. 所有主界面文案使用简体中文；品牌、快捷键和少量装饰性短标签可以使用英文。
4. 用户可以修改名称、目标和内容。演示中的职业、爱好、训练目标都只是可删除的示例。
5. 不擅自加入登录、付费订阅、团队协作、社交广场和消息推送。
6. 不调用收费 API，不创建云服务，不自行部署到公开网络。
7. 每完成一个阶段，运行与该阶段有关的检查，更新进度文档，再进入下一阶段。
8. 当前环境缺少浏览器、网络或执行能力时，明确标注未验证部分，不声称已经运行或测试通过。
9. 不用大量静态页面、硬编码数字和无效按钮制造完成的假象。
10. 需求发生冲突时，优先保证：数据正确、核心流程可用、可维护性、可读性、视觉效果。

## 二、版本范围与产品结构

### 2.1 首个完整版本需要实现

- 总览首页。
- 行动中心：目标、项目、任务、习惯和简易日程。
- 四个内置领域：个人提升、职业规划、兴趣爱好、运动健身。
- 知识库：笔记、资源链接、收藏和搜索。
- 周期复盘：周复盘、月复盘。
- 模块中心：创建普通模块，调整名称、颜色、图标、顺序和启用状态。
- 设置与数据管理：主题、个人偏好、时区、备份导出、备份恢复。
- 全局快速新增、站内搜索、专注计时。
- 响应式布局、空状态、错误状态、基础键盘操作。

### 2.2 后续版本再实现

以下项目只记录在路线图中，不要提前做出看起来可用的入口：

- 用户账号、跨设备同步、服务端数据库。
- AI 计划生成、AI 复盘和知识问答。
- 外部日历、可穿戴设备和第三方知识工具集成。
- 系统级通知、后台定时提醒。
- 文件附件、图片上传、复杂富文本编辑。
- 可任意拖拽缩放的仪表盘、完整表单设计器、插件市场。
- PWA 离线启动、桌面安装包、原生手机应用。
- 招聘信息抓取、简历自动投递、自动制定训练处方。

### 2.3 统一业务关系

以以下关系组织产品：

领域模块 → 目标 → 项目 → 任务 → 活动记录 → 周期复盘。

习惯是另一种持续行动，可以属于某个领域，并关联一个目标。笔记和资源可以关联领域、项目或目标。

这些关联允许为空。例如，一条临时任务可以进入「收集箱」，不必先建立完整目标体系。

四个内置领域复用统一的任务、目标、项目、笔记和活动记录，避免维护四套互不相通的数据。

## 三、信息架构与导航

### 3.1 桌面侧边栏

顶部：品牌标志、星枢 StarDesk。

第一组「工作空间」：

- 总览。
- 行动中心。

第二组「我的领域」：

- 个人提升。
- 职业规划。
- 兴趣爱好。
- 运动健身。
- 用户后来新增并启用的模块。

第三组「沉淀与回顾」：

- 知识库。
- 周期复盘。

底部固定区域：

- 模块中心。
- 设置与数据。
- 用户昵称及简短状态。

使用清晰的线性图标。侧边栏支持折叠并记住状态。当前页面有明显但克制的选中态。

### 3.2 顶部工具栏

- 面包屑或当前页面标题。
- 全局搜索入口，显示 Ctrl/Cmd + K。
- 快速新增按钮。
- 主题切换入口。
- 日期与简洁的本地保存状态。

不显示虚构的在线人数、服务器负载、云端同步成功等信息。

### 3.3 路由规划

至少包含下列可直接打开和刷新的页面：

- /dashboard
- /actions
- /modules/:moduleId
- /knowledge
- /reviews
- /module-center
- /settings

行动中心通过页签或查询参数区分任务、目标、项目、习惯、日程。领域详情通过页签区分概览、项目、记录和资料。

请根据目标运行环境正确处理路由刷新。若托管平台不能配置 SPA 回退，可采用 HashRouter，并在 README 中说明实际选择。

## 四、视觉设计规范

### 4.1 设计方向

关键词：深空、精密仪表、玻璃层次、冷色微光、轨道、秩序、安静的科技感。

整体要像一套成熟的个人指挥台，具有精致的未来感，同时适合连续阅读和每天使用。

科技感主要来自布局比例、字体层级、细线边界、少量发光焦点和有意义的数据反馈。

避免：

- 大面积高饱和紫色渐变。
- 满屏霓虹描边、持续闪烁、扫描线、下落代码雨。
- 默认播放音效、背景视频、复杂 3D 场景。
- 每张卡片都使用不同装饰语言。
- 为填充空间而添加无意义的雷达图、地球、地图和性能仪表。
- 过多玻璃模糊导致文字对比不足。
- 看起来像通用后台模板的密集表格首页。
- 落地页式巨大宣传标题和占满首屏的品牌横幅。

### 4.2 色彩 Token

请定义集中管理的 CSS 变量，组件全部引用语义 Token。

深色主题：

| Token | 建议值 | 用途 |
| --- | --- | --- |
| background | #080D16 | 页面底色 |
| surface | #101A29 | 普通卡片 |
| surfaceRaised | #162235 | 抽屉、弹窗、悬浮层 |
| border | #2B3D55 | 可见控件和边界 |
| divider | rgba(148,163,184,0.14) | 弱分隔线 |
| textPrimary | #EEF5FF | 正文和标题 |
| textSecondary | #A7B7CD | 辅助文字 |
| textMuted | #8495AC | 低优先级信息，仍须检验对比度 |
| accent | #5DE4F5 | 主操作、选中态、焦点 |
| accentSecondary | #8C9BFF | 少量辅助高光 |
| success | #5BD6A0 | 成功状态 |
| warning | #F2C46D | 临期和提醒 |
| danger | #FF8396 | 错误与破坏性操作 |

主按钮可使用青色实底与深色文字。不要在亮青色底上使用白色小字。

领域色建议：

- 个人提升：青蓝。
- 职业规划：靛蓝。
- 兴趣爱好：琥珀。
- 运动健身：薄荷绿。

领域颜色只用于图标、局部边线、标签和图表，不要给整个页面大面积染色。状态也要有文字或图标，不能只靠颜色区分。

浅色主题：

- 页面 #F3F6FB，卡片 #FFFFFF。
- 主文字 #17263C，辅助文字 #52647C。
- 强调色使用更深的青蓝，例如 #006F86。
- 使用独立 Token，检查图表、边框、禁用态、输入框和弹窗，不能简单反转背景色。
- 支持「深色 / 浅色 / 跟随系统」，首次默认为深色。

以上颜色是初始设计值。实际文字和控件必须通过对比度检查；发现问题时调整 Token，而不是坚持不合格的色值。

### 4.3 布局与尺度

- 桌面导航宽度约 232px，折叠后约 72px。
- 顶栏高度约 64px。
- 主内容最大宽度约 1560px，居中。
- 桌面内容边距 28–32px，平板 20–24px，手机 16px。
- 卡片间距以 16px 或 20px 为主。
- 普通卡片内边距 20–24px。
- 大卡片圆角 16px，小组件圆角 10–12px。
- 基础间距体系：4、8、12、16、20、24、32、40、48。
- 页面主标题 26–30px，卡片标题 16–18px，正文 14–16px，辅助文字 12–13px。
- 核心数字可以使用 28–36px，配合等宽数字。
- 中文优先使用系统字体栈，例如 PingFang SC、Microsoft YaHei、Noto Sans SC 的本地可用字体。
- 不依赖在线字体服务才能正确显示。

断点行为：

- 1280px 及以上：完整侧栏、12 栏内容网格，首页核心区域采用 8:4。
- 768–1279px：侧栏默认折叠或抽屉化，内容以双列或单列为主。
- 低于 768px：单列布局，导航进入抽屉；保留易触达的快速新增入口。
- 360px 宽度下仍能完成新增、编辑、打卡和查看复盘。
- 表格在手机上转成列表卡片，或只让表格区域横向滚动，页面整体不能横向溢出。

### 4.4 特色视觉组件

至少设计以下具有一致辨识度的组件：

1. 「今日航线」：首页主要工作区，展示最多三项今日重点及进度。
2. 「领域卡片」：用不同领域色、微型趋势、下一步行动呈现四个领域。
3. 「节奏轨道」：以清晰时间线显示今天的安排，时间标签使用等宽数字。
4. 「专注舱」：简洁环形计时器，静止时保持安静，运行时有轻微状态反馈。
5. 「成长足迹」：日历热力图或短期趋势图，必须有真实数据含义。

可以使用 CSS 背景网格和局部径向光晕，透明度约 2%–5%；装饰元素不截获鼠标、不覆盖文本，也不进入键盘焦点。

### 4.5 动效与可访问性

- 按钮、页签、悬浮反馈约 120–180ms。
- 抽屉和弹窗约 180–240ms。
- 优先使用 opacity 和 transform。
- 页面出现动效最多一次，不让仪表盘持续运动。
- 尊重 prefers-reduced-motion，关闭非必要位移和循环动画。
- 所有按钮可键盘访问，输入框具有真实 label。
- 图标按钮提供可理解的名称，键盘焦点清晰可见。
- 弹窗支持 Esc 关闭、焦点限制和关闭后焦点返回。
- 常规文字以至少 4.5:1 为对比度目标，大文字及必要非文本控件按相应可访问性要求检验。
- 手机主要触控目标建议至少 44×44px。
- 图表提供文字摘要，数值变化不能只靠颜色表达。

## 五、页面和功能的具体要求

### 5.1 总览首页

首页回答三个问题：今天做什么，近期进展如何，下一步从哪里开始。

布局建议：

第一行：简短问候、日期、星期、快速新增。

第二行：四个紧凑指标：

- 今日计划完成数 / 今日计划总数。
- 本周已完成专注时长。
- 今日习惯完成数 / 今日应打卡数。
- 当前进行中的目标数。

第三行主区域：

- 左侧 8 栏：「今日航线」，最多三项重点、普通今日任务、开始专注按钮。
- 右侧 4 栏：「今日节奏」，带时间的任务列表和当前专注状态。

第四行：

- 四个领域概览卡，展示一个主要指标、一个正在进行的项目、一个明确的下一步行动。
- 点击卡片或行动可进入对应领域或任务。

第五行：

- 最近 7 / 30 天投入趋势。
- 最近活动。
- 最近一次复盘或待开始的本周复盘。

细节：

- 今日重点由用户置顶，最多三项；超出时明确提示替换或取消一个。
- 临时任务进入收集箱后，可以一键加入今日计划。
- 今日计划使用明确的本地日期，不自动把所有到期任务都算作今日计划。
- Task.plannedDate 唯一决定某日计划的任务集合；DailyPlan 只保存该集合的顺序和重点。已完成任务保留在当日计划分母中，已取消和已删除任务不计入；分母为零时显示「尚未安排」。
- 昨日未完成任务不会自动挪到今天，用户可以主动改期。
- 逾期任务放在独立分组，可一键改期。
- 不使用神秘的「人生总分」或无解释的综合评分。
- 没有数据时显示「还没有安排今天的重点」及可操作按钮，不展示虚假的增长百分比。
- 图表和指标点击后能进入相应筛选结果。
- 首页组件支持显示/隐藏，以及通过上移/下移调整顺序；首版不要求自由拖拽画布。

### 5.2 行动中心

页签：任务、目标、项目、习惯、日程。

任务：

- 支持新增、编辑、完成、取消完成、取消任务、归档和移入回收站。
- 支持标题、说明、领域、所属项目、目标、优先级、计划日期、截止日期、预计时长、标签。
- 优先级使用「高 / 中 / 低」，不引入复杂权重。
- 状态：待办、进行中、已完成、已取消。
- 支持子任务，首版最多一层。
- 支持按关键词、领域、状态、优先级、日期筛选。
- 预设视图：收集箱、今天、未来七天、逾期、已完成。
- 支持列表视图和简单看板。看板至少支持通过菜单改变状态；拖拽属于后续增强。
- 批量操作至少支持完成、改期、归档。
- 详情使用右侧抽屉，手机上使用全屏面板。

目标：

- 支持标题、所属领域、描述、开始日期、目标日期、状态和进度模式。
- 进度模式只有两种：关联任务自动计算、手动百分比。
- 显示关联项目与任务，能够直接创建下一步任务。
- 目标状态：未开始、进行中、已完成、已归档。
- 自动进度及完成行为按第六节统一规则实现。

项目：

- 用来承载多步骤计划，例如读完一本书、准备一次求职、完成一件作品、执行一个训练周期。
- 支持名称、领域、目标关联、描述、起止日期、状态。
- 项目详情包含任务、笔记、资源和活动记录。
- 项目本身不重复存放另一份任务列表，只通过关联查询。
- 项目类型由领域模板提供默认值，核心行为保持一致。

习惯：

- 支持每天或每周指定星期的频率。
- 支持启用日期、结束日期、暂停和恢复。
- 一天一次二元打卡，支持取消打卡和补记过去日期。
- 记录日期不能晚于今天。
- 显示今日应完成、最近七天记录、本周完成率。
- 连续完成次数可以提供，但必须遵守第六节的计划日规则。
- 习惯名称、颜色可编辑；修改频率只影响未来计划，保留历史安排。

日程：

- 提供周视图和日列表视图。
- 来源为用户设置了计划日期或时间的任务。
- 全天任务与有具体时间的任务分开。
- 可以从空日期入口新增任务，任务改期后日程同步更新。
- scheduledTime 使用 HH:mm，填写时必须同时具有 plannedDate。它表示该计划日的日内开始时刻；切换时区时保留填写的日期与钟点，按新的应用时区解释。首版采用这种浮动本地日程，不承诺跨时区会议换算。
- 首版只显示计划开始时间，预计时长不是实际结束时间。
- 首版不做外部日历集成和复杂循环事件。

### 5.3 个人提升

定位：把学习方向变成可执行的学习计划，并沉淀所学内容。

页面内容：

- 当前学习目标。
- 正在进行的学习项目。
- 最近学习记录及累计学习时长。
- 相关技能自评。
- 最近笔记和学习资源。

具体功能：

- 学习项目类型：书籍、课程、技能、其他。
- 可设置目标日期、项目说明，并拆分章节或阶段任务。
- 学习记录包含日期、主题、投入分钟数、简短收获，允许关联项目。
- 资源类型：文章、视频、课程、书籍、其他；首版保存标题和链接。
- 在项目中新增笔记，默认带入领域和项目。
- 技能自评采用 1–5 级，附带自评说明和证据链接；不要宣称这是客观能力测评。

验收示例：创建「学习一项新技能」项目，添加三项任务，完成一项，记录 30 分钟学习并新增笔记；项目进度、首页和最近活动同步更新。

### 5.4 职业规划

定位：明确方向、看见能力差距、持续积累可以展示的成果。

页面内容：

- 当前职业方向与阶段目标。
- 职业发展路线，使用真实目标日期排列。
- 能力清单与当前/目标等级。
- 职业行动任务。
- 成果与证据。

具体功能：

- 使用目标和项目承载职业方向及阶段计划。
- 能力项支持名称、当前等级、目标等级、说明、证据链接。
- 差距由目标等级减当前等级得出，下限为零。
- 成果以笔记或资源形式记录，增加「职业成果」类型和相关标签。
- 职业任务可直接进入统一的今日计划。
- 每项能力可以关联一项学习项目，帮助把差距转成行动。
- 求职投递漏斗、工资预测、自动职业推荐放入后续版本。

不要自动为用户指定职业；演示可以使用「提升当前岗位的专业能力」等中性内容。

验收示例：建立一个职业目标和两个能力项，为其中一个能力关联学习项目；其下一步任务能在今日计划出现。

### 5.5 兴趣爱好

定位：帮助用户持续投入喜欢的事，并保存体验和作品。

页面内容：

- 兴趣项目。
- 本周兴趣投入时间。
- 最近实践记录。
- 收藏的灵感与作品。

具体功能：

- 兴趣项目可使用摄影、绘画、音乐、写作、园艺、其他等标签，用户可自行修改。
- 实践记录包括日期、项目、时长、体验或收获。
- 作品使用「作品笔记 + 外部链接」表示。
- 提供轻量卡片展示，用户未上传图片时使用统一排版和图标，不抓取不可靠的占位图片。
- 可设置每周投入目标，并展示已记录时长 / 目标时长。
- 对兴趣模块不使用强烈惩罚、连续失败、排行等压迫性表达。

验收示例：新增一个兴趣项目，记录两次实践并保存一个作品链接；页面展示两次实践的总时长和最新作品。

### 5.6 运动健身

定位：管理训练安排、记录实际完成的运动、观察个人趋势。

页面内容：

- 本周训练计划。
- 最近训练记录。
- 本周训练次数与时长。
- 可选的体重变化。

具体功能：

- 训练计划以项目承载，训练安排以任务承载。
- 运动记录包含日期、运动类型、时长、主观强度、备注。
- 运动类型支持力量、跑步、骑行、游泳、瑜伽、步行、其他。
- 跑步、骑行、步行可选填距离；力量训练可在备注中记录内容。
- 距离使用 km，体重使用 kg，时长使用分钟，单位显示清楚。
- 体重记录为可选项，首版不强制填写身体信息。
- 从训练任务点击「记录完成」时，可以填写运动记录并完成该任务。
- 普通勾选任务完成不会自动编造运动时长或体重。
- 不根据少量数据自动给出医疗判断、热量处方或未经用户设定的训练强度。

动作库、组数重量管理、训练周期算法、身体照片和设备连接放在后续版本。

验收示例：创建本周两次训练任务，记录一次 35 分钟训练；任务完成状态、训练统计、首页最近活动一致。

### 5.7 知识库

页面提供笔记和资源两个页签。

笔记：

- 标题、Markdown 正文、标签、领域、项目/目标关联、置顶和归档。
- 支持编辑与预览，安全处理 Markdown 内容。
- 有未保存修改时关闭编辑面板，应保留草稿或提示处理。
- 列表支持最近修改、领域、标签和关键词筛选。
- 提供笔记详情深链接。

资源：

- 标题、链接、类型、说明、标签、所属领域或项目。
- 校验链接格式，外链打开使用适当安全属性。
- 支持收藏和「待查看 / 已查看」状态。

首版搜索覆盖标题、标签和可搜索的正文；搜索结果标明类型、领域和更新时间。

### 5.8 周期复盘

提供周复盘和月复盘，不必另做繁重的每日问卷。

复盘页面：

- 周期范围。
- 该周期任务完成数量。
- 学习、兴趣和运动的实际投入。
- 习惯计划达成情况。
- 各领域的简明数据摘要。
- 用户填写的复盘内容。

默认复盘问题：

1. 这段时间最值得保留的进展是什么？
2. 哪些事情没有按预期推进，原因是什么？
3. 哪些投入让我更有能量？
4. 下一周期最重要的一到三件事是什么？

支持保存草稿、标记完成、重新编辑。

「下一周期行动」可以显式转换为任务。转换前显示标题、日期和领域，确认后创建真实任务，并防止重复点击造成重复任务。

首次完成复盘时保存统计快照及统计时间。后来修改历史记录时，已完成复盘继续显示快照，并提供用户主动更新快照的入口。

不把预写文案标记为「AI 分析」。首版只提供结构化模板和真实统计。

### 5.9 模块中心

支持：

- 查看四个内置领域及自定义领域。
- 修改显示名称、描述、图标、颜色。
- 使用上移/下移调整顺序。
- 启用、停用、归档、恢复。
- 创建普通自定义模块。

创建普通模块时填写：

- 名称。
- 描述。
- 图标和颜色。
- 启用的能力：目标、项目、任务、习惯、笔记、活动记录。

例如新增「旅行计划」，就能拥有旅行目标、出行准备任务、攻略笔记和活动记录。

首版普通模块无需写代码即可创建，使用现成字段和页面模板。新增机票比价、地图路线、账务核算等专属行为，需要后续开发新的功能适配器。

不要声称输入一个模块名称就能自动获得任意业务能力。

### 5.10 设置与数据管理

至少包括：

- 昵称、工作台名称。
- 深色/浅色/跟随系统。
- 界面密度：舒适/紧凑。
- 时区，默认读取设备时区，读取失败使用 Asia/Shanghai。
- 每周开始日，默认周一。
- 专注时长偏好。
- 首页显示的组件及顺序。
- JSON 备份导出、JSON 备份恢复。
- 回收站及恢复。
- 演示空间管理。
- 数据存储说明和最近一次发起导出时间。

清空个人数据必须由用户从设置主动操作，显示涉及范围并确认；开发调试和数据库升级不得自动清空。

## 六、核心交互与统计规则

### 6.1 快速新增与搜索

- Ctrl/Cmd + K 打开搜索/命令面板。
- 输入框或编辑器获得焦点时，不能抢占正常输入快捷键。
- 快速新增至少支持任务、笔记、活动记录和项目。
- 从某个领域页面打开时默认带入该领域。
- 从项目页面打开时默认带入领域和项目。
- 新增表单只优先展示必填字段，高级字段可以折叠。
- 搜索面板支持上下选择、Enter 打开、Esc 关闭。
- 无结果时显示清晰的空状态，并允许直接新建对应内容。
- 搜索结果打开真正的详情，返回时保留原筛选条件。

### 6.2 数据操作体验

- 保存中禁止重复提交，显示真实的进行状态。
- 只有数据库写入成功才显示「已保存」。
- 保存失败保留输入内容，给出可重试的错误提示。
- 删除采用可恢复的软删除，回收站支持恢复；永久删除只通过明确操作进行。
- 归档、完成、停用、删除具有不同语义，不能混为一谈。
- 批量操作显示影响数量。
- 对普通可逆操作不要反复弹出确认框。
- 长标题、空说明、大量标签和中文换行均需验证。

### 6.3 目标与项目进度

- 自动模式进度 = 已完成有效任务数 / 全部有效任务数。
- 已取消、已删除的任务不计入分子或分母。
- 一个任务关联同一目标和该目标的项目时，只按唯一 ID 统计一次。
- 有子任务的父任务不再与子任务重复计权；首版按可执行的叶子任务统计。
- 父任务的完成状态由子任务决定；全完成时完成，取消子任务完成时父任务重新打开。
- 没有有效任务时显示「尚未拆解」，不要显示 100%。
- 手动模式限制在 0–100，并注明「手动更新」。
- 手动填写进度不会批量修改任务状态。
- 自动进度到达 100% 时，提示用户确认目标完成；不强制更改整个目标的业务状态。
- 目标完成后仍可重新打开，保留原有关联内容。

### 6.4 日期、时间与历史口径

- 绝对时间使用 UTC ISO 时间戳保存，例如创建时间和专注开始时间。
- 纯日期使用 YYYY-MM-DD 字符串保存，例如任务计划日、截止日、打卡日。
- 用统一的日期工具根据设置中的 IANA 时区计算今天、周区间和月区间。
- 不用 toISOString().slice(0,10) 代替用户当地日期。
- 修改时区不挪动已经保存的纯日期记录；绝对时间按新的时区展示。
- 活动记录的发生日期由用户确认，保存后保持稳定。
- 本周从设置中的周开始日计算，不能在不同页面采用不同口径。
- 已完成任务记录 completedAt 和 completedLocalDate；重新打开时清除当前完成字段，再次完成时按新完成时间统计。
- 首版不做完整事件溯源，因此历史任务统计按当前记录计算；已完成复盘的快照保持不变。
- 已取消、已完成和已删除任务不标记为逾期。
- 只有有截止日期且截止日期早于今天的未完成任务才显示逾期。

### 6.5 习惯口径

- 同一习惯同一本地日期只能有一条有效打卡记录，数据库层保证唯一性。
- 每日应打卡数只包含当天计划内、已启用、未暂停且未归档的习惯。
- 历史计划通过带生效日期的频率版本和暂停区间计算，修改未来频率不重写过去。
- 本周达成率的分母仅统计本周截至今天已经到来的应打卡机会。
- 没有应打卡机会时显示「本周暂无安排」，不要除零。
- 连续次数按连续的应执行日期计算，休息日不打断。
- 今天尚未结束且未打卡时，不提前判定历史连续记录中断。
- 补记会重新计算相关显示，不能插入未来打卡。

### 6.6 专注计时

- 提供 25、45、60 分钟预设和自定义时长。
- 支持关联任务、开始、暂停、继续、结束和放弃。
- 使用已结算运行片段、当前片段起点和目标结束时间计算剩余时间，保留原始开始时间。
- 统一采用运行片段模型：accumulatedActiveSeconds 保存已结算片段秒数，activeSegmentStartedAt 保存当前片段起点。暂停时结算片段并清空起点；继续时建立新起点，并按剩余秒数重算 targetEndAt。不要混用另一套暂停时长算法。
- setInterval 只负责刷新显示，不作为时间真实性来源。
- 切换页面和短时后台后时间应正确，刷新后能恢复进行中状态。
- 浏览器关闭后无法保证提示音或通知，不做相反承诺。
- 重新打开时若计划结束时间已过，展示待确认的已结束会话，允许确认实际时长或放弃；不要静默当作用户一直专注。
- 同时仅有一个活动会话；启动检查和写入必须通过事务避免双标签页重复创建。
- 结束时只生成一次专注记录，重复点击不会重复计时。
- 状态明确区分运行中、暂停、待确认、已完成、已放弃、恢复待确认。只有已完成记录进入统计；结束结算和状态变更在同一事务中执行。
- 倒计时到达目标结束时间后进入待确认，不能把用户过了很久才回来确认的等待时间算入专注时长。
- 确认结束时让用户确认统计日期，默认是应用时区下的结束日期；localDate 保存后不随时区变更。首版整段归入该日期，不跨日拆分。
- 专注结束不自动完成关联任务。
- 系统时间明显变化时提示用户检查本次记录。

### 6.7 时长与图表

- 首页「专注时长」只统计有效完成的 FocusSession。
- 各领域「投入时长」统计该领域的活动记录。
- 专注完成后允许用户显式转成领域活动；活动保留 sourceFocusSessionId，并防止同一专注会话重复转换。
- 专注与领域投入是两个不同指标，不把它们直接相加得出总时长。
- 训练次数按有效运动记录数计算，完成训练任务本身不增加运动次数。
- 首版体重图同一天保留一条有效记录，新输入时进入明确的更新流程。
- 时间序列按日期排序、补齐无活动日期为零；体重等测量值缺失时保留缺口，不补零。
- 筛选区间、单位、计算口径要在图表附近可见。
- 所有统计由统一 selector/service 计算，不在每个页面各写一份算法。

## 七、技术方案与工程约束

### 7.1 默认技术栈

对于空项目，采用以下默认方案：

- React + TypeScript + Vite。
- React Router 处理页面路由。
- Tailwind CSS + CSS 变量管理样式。
- Dexie 封装 IndexedDB，持久化所有核心数据。
- Zod 验证表单和备份文件。
- Lucide React 提供统一图标。
- 图表优先使用一套轻量且成熟的方案，例如 Recharts，按需加载。
- 日期工具集中封装，可使用 date-fns 及与实际版本兼容的时区方案。
- Vitest 验证核心业务规则；React Testing Library 验证必要组件行为；Playwright 验证关键端到端流程。

选择当前环境可安装且彼此兼容的稳定版本，记录实际依赖版本并保留锁文件。不要凭记忆指定「最新版」，不要混用不同主版本的配置方式。

React 官方提供了基于 Vite 从零搭建客户端应用的路径；该方案用于本项目单人本地使用的首版。[React 官方说明](https://react.dev/learn/build-a-react-app-from-scratch)

Tailwind 配置应对应实际安装版本；采用当前 Vite 集成时查阅其官方安装说明。[Tailwind 官方说明](https://tailwindcss.com/docs/installation/using-vite)

Dexie 提供 IndexedDB 数据访问及事务能力；备份采用本项目明确的版本化格式和校验流程。[Dexie 官方文档](https://dexie.org/docs/)

如果现有项目已采用其他合理的前端框架，先复用现有技术栈，并映射本文中的分层与验收要求，避免无必要迁移。

### 7.2 分层要求

至少分离：

1. 视图层：页面、表单、列表、弹窗、图表。
2. 业务层：目标进度、习惯计划、日期规则、统计、关系约束。
3. 数据层：数据库定义、版本迁移、仓储、事务和导入导出。
4. 模块层：模块定义、路由适配、页面模板、首页摘要。

组件不直接拼装数据库操作；业务规则尽量写成可测试函数。业务数据以数据库为唯一持久化来源，不在全局状态和数据库中维护互相竞争的副本。

UI 临时状态优先使用 React 状态或 Context。只有出现真实共享需求时再引入额外状态库。

localStorage 只可保存主题预加载、侧栏折叠等轻量 UI 偏好。核心任务、笔记、计划和记录保存到 IndexedDB。

开发版可使用 React StrictMode，数据初始化和副作用必须可重复执行，不重复插入演示记录或活动记录。

### 7.3 建议目录

```text
src/
  app/                  应用入口、路由、全局 Provider
  components/
    ui/                 通用可访问性组件
    layout/             侧栏、顶栏、页面框架
    shared/             任务项、实体选择器、空状态
  features/
    dashboard/
    actions/
    goals/
    projects/
    habits/
    focus/
    knowledge/
    reviews/
    module-center/
    settings/
  modules/
    registry.ts
    shared/
    growth/
    career/
    hobbies/
    fitness/
    generic/
  domain/               实体类型、校验、业务规则
  data/
    db/
    repositories/
    migrations/
    backup/
    demo/
  lib/                  日期、ID、格式化等工具
  styles/               Token、全局样式、主题
  tests/
docs/
  PRODUCT_SPEC.md
  TASKS.md
  HANDOFF.md
README.md
```

可以按现有项目适度调整，但不要把数据库、所有页面和复杂业务规则写进一个文件。不要为了目录完整性生成大量没有用途的空文件。

## 八、核心数据模型与关系约束

### 8.1 通用字段

持久化业务实体使用稳定的 UUID：

- id
- createdAt
- updatedAt
- revision：用于检测陈旧编辑，初始为 1，每次有效修改递增。
- deletedAt：可空，表示进入回收站。
- deleteBatchId：可空，关联同一次软删除操作。

适用实体再增加 archivedAt，不在所有实体上机械添加没有意义的字段。

枚举值在数据层使用稳定英文值，界面通过映射显示中文。名称、图标和排序都不能作为实体身份。

下面列出的字段是最低契约；可补充实现所需字段，但应先更新文档，再修改代码。

### 8.2 主要实体

| 实体 | 最少字段 |
| --- | --- |
| ModuleConfig | id、definitionKey、name、description、iconKey、colorKey、order、enabled、capabilities、archivedAt |
| Goal | moduleId、title、description、startDate、targetDate、status、progressMode、manualProgress |
| Project | moduleId、goalId?、title、description、projectType、startDate、targetDate、status、weeklyTargetMinutes? |
| Task | moduleId?、goalId?、projectId?、parentTaskId?、title、description、status、priority、plannedDate?、scheduledTime?、dueDate?、estimatedMinutes?、tags、completedAt?、completedLocalDate? |
| DailyPlan | localDate、orderedTaskIds、topTaskIds |
| Habit | moduleId、goalId?、title、description、startDate、endDate?、scheduleVersions、pauseIntervals |
| HabitCheckin | habitId、localDate、note? |
| FocusSession | taskId?、moduleId?、status、plannedSeconds、startedAt、targetEndAt?、activeSegmentStartedAt?、pausedAt?、accumulatedActiveSeconds、endedAt?、actualSeconds?、localDate? |
| ActivityLog | moduleId、projectId?、taskId?、kind、localDate、title、durationMinutes、note、sourceFocusSessionId?、details |
| SkillAssessment | moduleId、name、currentLevel、targetLevel、description、evidenceUrls、learningProjectId? |
| BodyMeasurement | moduleId、localDate、weightKg、note? |
| Note | moduleId?、projectId?、goalId?、title、bodyMarkdown、noteType、tags、pinned、archivedAt |
| Resource | moduleId?、projectId?、title、url、resourceType、description、tags、readStatus、favorite、archivedAt |
| Review | periodType、startDate、endDate、status、answers、statsSnapshot?、snapshotAt?、nextActions、convertedTaskIds |
| UserSettings | nickname、workspaceName、theme、density、timeZone、weekStartsOn、focusPreferences、dashboardLayout、onboardingCompleted、lastExportInitiatedAt? |

表中 ? 表示可选，实际代码使用明确的可选或可空约定，避免 undefined、null、空字符串混用。

ActivityLog.details 使用有类型的联合结构：

- learning：学习主题、收获，可关联资源。
- hobby：实践内容、作品链接。
- workout：运动类型、主观强度、可选距离。
- generic：普通活动说明。

不要让 details 接受任意可执行代码，也不要把所有实体都塞进没有校验的任意 JSON。

职业成果使用 noteType = achievement；兴趣作品使用 noteType = work。领域页面直接查询统一知识库数据。

### 8.3 必须验证的约束

- 标题去除前后空格后不能为空，建议最多 120 字。
- 描述、正文和单次导入文件设合理的大小限制，并在界面说明。
- 分钟数和重量为有限数字；时长非负，体重大于零；缺失值保留为空，不转换为零。
- 百分比范围 0–100，技能等级 1–5。
- 起始日期不能晚于目标日期。
- 链接只接受安全且明确的协议，如 https 和 http，拒绝 javascript 等协议。
- 项目、任务和目标的主领域关系保持一致；关联不兼容目标时由表单阻止并解释。
- 任务关联项目时继承该项目的 goalId，表单不可另选一个不同目标；没有项目的任务可以直接关联目标。项目改目标时在事务中同步关联任务，项目无目标时其任务也不单独绑定目标。
- 职业能力项关联学习项目属于支持关系，允许跨领域；不改变项目或任务的主领域归属。
- 子任务继承父任务的领域、项目和目标，不能形成循环，也不能再创建下一级子任务。
- 目标或项目换领域时，明确迁移哪些关联实体并使用事务；不静默复制一套任务。
- 任务进入今日计划和今日重点时，同一天不能重复出现同一 ID。
- DailyPlan 按 localDate 唯一；先查询该日期的有效任务，再应用 orderedTaskIds，未列入顺序的任务按创建时间追加。topTaskIds 必须属于当天有效任务集合且最多三项。
- 任务改期、移除计划日期、取消或删除时，在同一事务中清理旧日期的顺序和重点引用；导入时校验对应关系。
- 同一 Habit + localDate 唯一。
- 同一 Module + localDate 的体重记录唯一。
- 同一 FocusSession 最多转成一条 ActivityLog。
- 相同周期类型与日期范围最多有一份有效复盘。
- 模块、主题和图标配置使用白名单映射，不把用户文本直接当类名或组件路径执行。

### 8.4 删除、归档与停用

- 停用模块只隐藏导航入口；默认总览的领域卡隐藏该模块，但其历史数据仍可在行动中心通过筛选找到。
- 模块归档后默认不再接受新内容；关联历史可读、可恢复。
- 模块首版不提供一键永久删除所有关联内容。
- 删除目标不删除任务、项目和笔记。软删除期间保留引用，界面显示「关联目标已删除」，并允许恢复或解除关联。
- 删除项目不静默删除其任务。软删除时保留关系；永久删除时先展示影响，并在事务中解除相关引用。
- 父任务删除时显示子任务数量，首版选择将父子一起软删除；恢复时恢复同批删除的内容。
- 同批删除由 deleteBatchId 确定。只给父任务及本次尚未删除的子任务写入新批次 ID，原先已删除的子任务保留原批次；恢复父任务只恢复本批次。单独恢复子任务前，其父任务必须已恢复。
- 永久删除必须处理反向引用，不留下指向不存在实体的 ID。
- 归档仅影响默认视图；普通历史统计仍可包含符合时间条件的归档记录。
- 已删除数据从实时统计排除；已完成复盘的历史快照保持原样。

### 8.5 多标签页与迁移

- 数据变更在其他标签页可见，使用 Dexie 响应式查询或明确的通知机制。
- 提交编辑时比较 revision；若已被其他页面修改，保留当前输入并提示重新加载或处理冲突，不直接静默覆盖。
- 每个数据空间保存本地控制字段 dataEpoch，完整替换恢复时生成新值，不从备份原样恢复。编辑事务同时比较打开表单时的 dataEpoch 和 revision，避免恢复后旧标签页恰好以相同 revision 覆盖新数据。
- dataEpoch 变化后通知其他标签页，使缓存失效、停止旧会话计时并阻止旧表单直接提交；保留输入，提示重新读取恢复后的数据。
- 数据库迁移使用明确版本号和增量迁移。
- 从阶段 1 起建立迁移入口；后续每次新增持久化表或改变字段都同时提供迁移和旧数据验证，不等到最后阶段才处理。
- 升级被旧标签页阻塞时给出重新加载提示。
- 迁移失败保留原数据并报告错误，禁止通过删库掩盖问题。
- 跨设备同步与复杂自动冲突合并不属于首版。

## 九、模块扩展架构

### 9.1 开发层模块定义

维护唯一的模块注册表，定义类似：

```ts
type ModuleCapability =
  | "goals"
  | "projects"
  | "tasks"
  | "habits"
  | "notes"
  | "activities";

interface ModuleDefinition {
  key: string;
  defaultName: string;
  iconKey: string;
  defaultColorKey: string;
  capabilities: ModuleCapability[];
  overviewComponent: React.ComponentType<{ moduleId: string }>;
  getSummary: (context: ModuleSummaryContext) => Promise<ModuleSummary>;
}
```

这段接口只表达契约方向。实际实现补齐导入、类型定义和运行环境，不得留下无法编译的类型占位符。

ModuleDefinition 存在代码里；用户可编辑的 ModuleConfig 存在数据库里。React 组件和函数不能序列化进数据库或 JSON 备份。

导航、领域列表和首页卡片读取同一模块配置。不能在三个位置手写四个内置模块名单。

### 9.2 普通模块

- 新建时使用 generic 定义和新的稳定 ModuleConfig ID。
- 复用目标、项目、任务、习惯、笔记、活动能力。
- 路由、搜索、首页摘要、导入导出自动识别新模块。
- 修改模块名称不会使原有关联或路由失效。
- 模块颜色通过预设色板选择。
- 不支持运行时执行用户脚本。

### 9.3 后续专属模块

以后开发旅行路线、财务账本、饮食日志等专属模块时：

1. 新建对应模块目录和业务规则。
2. 注册新的 ModuleDefinition。
3. 优先复用通用实体，确有必要再增加专属实体。
4. 若增加持久化结构，提供迁移、校验、导入导出适配。
5. 接入搜索、摘要、导航和归档行为。
6. 验证原有四个领域的数据与流程继续可用。

不要现在实现通用脚本引擎、动态代码生成或复杂插件沙箱。

## 十、数据持久化、备份与演示空间

### 10.1 保存范围

业务数据保存在当前浏览器、当前站点来源下。首次引导和设置中用一句易懂的说明告诉用户：

「数据保存在这个浏览器中。更换浏览器、设备或访问地址前，请先导出备份。」

浏览器存储通常按来源隔离，且可能被用户或浏览器清理，因此本地保存不能等同于独立备份。[MDN 存储说明](https://developer.mozilla.org/en-US/docs/Web/API/Storage_API/Storage_quotas_and_eviction_criteria)

首版不依赖云端即可读写已加载应用的数据，但没有实现 Service Worker 时，不能承诺断网后重新打开页面也能启动。

数据写入失败时保留输入。遇到存储不可用或配额异常，应提示错误和可采取的操作，不能自动转为临时内存后继续显示「已保存」。

### 10.2 备份格式

导出可读 JSON，至少包含：

```json
{
  "appId": "stardesk",
  "exportFormatVersion": 1,
  "schemaVersion": 1,
  "exportedAt": "UTC ISO timestamp",
  "timeZone": "Asia/Shanghai",
  "sourceSpace": "personal",
  "recordCounts": {},
  "data": {}
}
```

上面是格式示意。实际代码必须输出真实时间、实际版本、实际计数和完整实体集合。

- 导出包含所有业务表、模块配置、习惯历史、归档及回收站内容和用户设置。
- exportFormatVersion 表示外层文件格式版本，schemaVersion 表示实际数据结构版本，两者独立维护。只有明确支持的旧版本转换可以补齐后来新增的实体集合，不能对任意缺表文件静默补空。
- 数据库读取采用一致性快照，不在用户修改过程中拼接互相矛盾的多张表。
- 不导出函数、UI 临时状态和访问密钥。
- 备份文件名包含产品名和导出日期。
- 界面写「备份文件已生成」或「已发起下载」，浏览器未提供证据时不声称文件已经安全写入某个目录。
- JSON 备份为明文，不标记为加密备份。

### 10.3 恢复流程

首版只做「完整替换恢复」，不做含糊的智能合并。

步骤：

1. 读取文件，在写入数据库前完成解析。
2. 校验 appId、格式版本、结构版本、字段、枚举、大小限制。
3. 校验每表 ID 唯一性、实体引用、唯一业务约束及模块定义是否受支持。
4. 支持的旧版本先经过明确转换；无法理解的新版本直接拒绝。
5. 展示预览：模块数、各类记录数、导出时间、数据来源、将替换的当前数据数量。
6. 提供「先导出当前数据」入口。
7. 用户明确确认替换后，使用单个写事务完成全部替换。
8. 任意表写入失败，整个事务回滚，原有数据继续可用。
9. 成功后重新读取设置和数据，核对计数并刷新界面。

替换事务同时更新本地 dataEpoch；旧标签页的编辑和计时必须失效。恢复期间阻止新的写入，从确认恢复到事务完成显示明确状态。

错误文件、重复 ID、悬空引用、未知必需字段格式或不支持的版本均不得部分写入。未知模块能力不能被静默丢弃。

导出包含进行中或暂停的专注会话时，恢复后统一标记为「待确认恢复」，不自动继续倒计时，也不重复生成专注记录。

### 10.4 演示与正式数据分离

首次启动给出两个明确选择：

- 创建我的空白工作台。
- 进入演示空间。

演示空间与个人空间使用独立数据库或严格隔离的数据命名空间。切换不复制记录。

演示空间始终有明显标识，可以重置；个人空间不会自动播种示例数据。

建议准备最近 2–4 周的连贯示例：

- 4 个内置领域。
- 4 个目标。
- 5–6 个项目。
- 15–20 个任务，包含待办、进行中、已完成、逾期和无日期状态。
- 3 个习惯及部分历史打卡。
- 若干学习、兴趣和运动记录。
- 5–8 条笔记和资源。
- 1 份完成的周复盘、1 份草稿。

示例日期相对初始化日期生成，不能让页面多年后仍停在固定示例年份。演示 ID 稳定、初始化幂等，刷新不重复注入。

演示数据从导出文件中的 sourceSpace 可识别。把演示备份恢复到个人空间时，在确认页面明确提示来源。

## 十一、状态设计与性能要求

### 11.1 必须覆盖的界面状态

每个主要页面考虑：

- 首次使用。
- 空数据。
- 正常内容。
- 筛选后无结果。
- 本地数据库初始化。
- 保存中与保存失败。
- 长标题和长正文。
- 已删除或不存在的详情。
- 模块停用或归档。
- 深色、浅色、窄屏。

空状态文字具体，例如：

- 「还没有学习计划。先选一个你想掌握的主题。」
- 「本周还没有运动记录。完成一次活动后，可以在这里留下记录。」
- 「没有符合这些条件的任务。清除筛选后再看看。」

不要让空页面只剩「暂无数据」四个字，也不要用假图表填充空白。

### 11.2 性能

- 按路由拆包，图表和 Markdown 预览按需加载。
- 为 moduleId、计划日期、状态、关联 ID、更新时间等实际查询条件建立合适索引。
- 首页一次聚合读取，不为每张卡重复加载全部数据库。
- 搜索加入适度防抖；大列表分页或按需渲染。
- 用具有代表性的约 1,000 条任务和活动记录检查交互，报告实际环境，不编造基准分数。
- 不为视觉效果加入大体积视频、3D 库或持续重绘。
- 外链资源加载失败不能破坏整个工作台。
- 使用统一错误边界，出现错误时给出恢复入口。

## 十二、分阶段实施计划

按依赖顺序开发。每阶段结束后保证项目仍然可启动，记录真实检查结果。用户没有要求阶段停下时，可以在验证通过后继续下一阶段。

### 阶段 1：工程、设计系统和任务闭环

实现：

- 检查或初始化工程。
- 创建 docs/PRODUCT_SPEC.md、docs/TASKS.md、docs/HANDOFF.md。
- 完成主题 Token、应用框架、侧栏、顶栏、路由。
- 定义数据库初始版本及 ModuleConfig、Task、UserSettings。
- 建立模块注册表契约、四个内置模块定义和版本迁移入口；定义个人/演示空间的隔离方式。后续阶段直接扩展，不重新迁移架构。
- 实现任务新增、编辑、完成、重新打开、软删除和基本筛选。
- 完成总览核心区域，使用真实任务；未来功能保留在文档，不暴露可误点击的虚假按钮。
- 实现正确空状态和基本手机适配。

验收：从空白工作台创建任务，完成后刷新，内容与状态保留；主要页面可打开；构建和类型检查通过。

### 阶段 2：统一行动系统

实现：

- 目标、项目和实体关联。
- 子任务、今日计划、最多三项重点。
- 习惯、频率历史、打卡和简单日程。
- 统一统计与日期工具。
- 任务详情抽屉和批量操作。

验收：目标 → 项目 → 任务 → 今日执行的流程贯通；自动进度无重复计算；打卡唯一；跨天和周边界测试通过。

### 阶段 3：四个领域与知识库

实现：

- 四个领域模板。
- 学习、兴趣、运动活动记录。
- 职业能力自评。
- 可选体重记录。
- 笔记、资源及关联。
- 领域摘要与首页真实汇总。

验收：四个领域分别完成第五节中的示例流程；同一笔记录在不同页面显示一致；不存在四套重复任务数据。

### 阶段 4：日常效率与复盘

实现：

- 全局搜索和快速新增。
- 专注计时及刷新恢复。
- 周复盘、月复盘、统计快照和行动转换。
- 真实趋势图与活动时间线。

验收：搜索打开详情；专注结束不重复写记录；复盘行动只创建一次；复盘快照不被后来历史编辑静默修改。

### 阶段 5：模块扩展与数据可靠性

实现：

- 完善前期建立的模块注册表，开放普通自定义模块的用户管理能力。
- 模块启用、停用、顺序、名称、图标和颜色。
- 首页组件显示与顺序设置。
- JSON 导出与完整恢复。
- 完善回收站、多标签页冲突检测及数据恢复时的控制；核对前面各阶段已随结构变更交付的迁移。
- 正式空间与演示空间分离。

验收：新增「旅行计划」普通模块后可立即使用通用功能；改名不丢数据；导出恢复后实体、关联和设置一致；损坏导入不改变旧数据。

### 阶段 6：视觉打磨、综合验证与交付

实现：

- 统一卡片、表单、图表、弹窗和交互状态。
- 完成 360、390、768、1024、1440、1920px 的重点页面检查。
- 检查深浅主题、键盘焦点、减少动效。
- 修复全流程问题。
- 完善 README 与交接文档。

验收：全部关键流程真实运行，检查结果分类清楚，已有数据继续可用，交付可启动的应用和准确运行说明。

## 十三、验收标准

### 13.1 自动化检查重点

至少围绕这些容易出错的业务规则提供有效测试：

1. 目标进度去重、子任务计权和空分母。
2. 日期与时区、月末/周末、午夜前后、无截止日期的任务。
3. 习惯计划日、暂停、历史频率变更、同日唯一性。
4. 专注计时恢复、重复结束和重复转换活动。
5. 备份结构校验、引用校验、版本处理和导入事务回滚。
6. 复盘行动转换的幂等性。

测试不要只断言组件存在，或复制实现公式得到同一结果。使用能区分正确与错误行为的输入和边界案例。

### 13.2 关键用户流程

必须实际验证：

- 空白工作台 → 创建目标和项目 → 添加任务 → 加入今日重点 → 完成 → 首页及领域同步更新。
- 关闭再打开应用 → 数据、主题、导航状态仍然存在。
- 修改任务 → 从首页、项目页和搜索详情查看同一更新。
- 习惯同日多次点击 → 只有一条有效打卡；取消后状态恢复。
- 记录训练 → 训练次数和时长正确；只完成任务不产生虚构时长。
- 从专注记录生成学习记录 → 重试不重复创建。
- 创建周复盘 → 转换下一周行动 → 首页可见，复盘保留任务链接。
- 创建普通模块 → 添加内容 → 改名 → 停用 → 恢复，数据完整。
- 导出 → 在隔离测试空间恢复 → 每表计数、关系、配置、统计一致。
- 损坏 JSON、重复 ID、未知版本、丢失引用 → 导入拒绝且原数据不变。
- 模拟写入失败 → 输入保留，没有假成功提示。
- 两个标签页编辑同一内容 → 能发现陈旧版本，不静默覆盖。
- 软删除 → 回收站恢复 → 关联内容正确返回。

### 13.3 视觉验收

- 首页有清晰主次，今日行动处于视觉中心。
- 四个领域保持统一设计语言，且有可识别的领域差异。
- 不出现文字重叠、内容截断、页面横向溢出和弹窗超屏。
- 每个主要交互有真实反馈。
- 深色和浅色主题均可阅读。
- 模块增多时导航仍可用。
- 真正测试长标题、空数据和较多数据，不只检查理想示例首页。
- 如环境允许，提供首页、四个领域、模块中心、手机首页的实际截图。
- 截图必须来自当前代码，不能拿设计稿冒充运行结果。

### 13.4 运行检查

依据项目实际脚本运行：

- 类型检查。
- Lint。
- 生产构建。
- 关键业务单元测试。
- 关键端到端流程。

环境不支持某项时说明缺失的能力、未验证范围和可复现步骤。不能为让检查通过而删除功能、弱化类型约束或注释关键测试。

## 十四、开发文档、交接与最终报告

维护以下最小文档集合：

### docs/PRODUCT_SPEC.md

- 产品范围。
- 统一数据关系与关键统计规则。
- 模块扩展约定。
- 已确认的实现决策。
- 首版明确后置的能力。

### docs/TASKS.md

- 按六个阶段列出任务。
- 状态仅使用：未开始、进行中、已验证完成、已实现待验证、阻塞。
- 每个完成项附简短验证证据。
- 不把静态占位页面登记成完整功能。

### docs/HANDOFF.md

每轮工作结束时更新：

- 当前阶段和当前可运行状态。
- 实际技术栈、包管理器、运行命令。
- 本轮改动的关键文件及原因。
- 数据库和备份格式版本。
- 已执行的检查、结果、未执行原因。
- 已知问题和剩余任务。
- 下一步最具体的一到三项操作。
- 需要下一位开发 AI 保持的设计和数据约束。

### README.md

用不要求开发经验的语言说明：

- 环境要求与安装依赖命令。
- 如何启动开发版。
- 如何构建并预览生产版。
- 当前数据存在哪里。
- 如何进入演示空间和个人空间。
- 如何导出、恢复和迁移到新的浏览器访问地址。
- 首版限制和后续路线。

最终报告必须区分：

1. 已实现且验证的功能。
2. 已实现但当前环境未验证的功能。
3. 尚未实现或明确后置的功能。

同时给出实际运行地址或启动方式、验证结果和必要截图。

现在请先检查环境，用简短清单说明执行顺序，随后开始开发。不要只输出策划方案，也不要在可以继续的情况下反复询问是否继续。

---

## 附录 A：启动开发提示词

将完整文档交给开发工具后，发送下面这段：

```text
请读取我提供的《星枢 StarDesk：个人工作台完整开发提示词》，将它作为本项目的产品、设计和验收规范。

请实际开发这个应用。我完全依赖 AI 工具完成开发，因此你需要自行处理常规工程和设计决策，提供可运行成果，并保持清晰的项目交接文档。

先检查当前目录和现有工程，保留已有有效代码；空项目采用文档中的默认技术栈。把规范中的核心决策整理进 docs/PRODUCT_SPEC.md，建立 docs/TASKS.md 和 docs/HANDOFF.md。

按照文档的六个阶段执行。先做好未来感视觉框架和一个真实的任务持久化闭环，再依次完成通用行动系统、四个领域、知识库、复盘、模块扩展和数据备份。

每阶段运行相应检查，修复问题后继续。每轮只推进清晰且可验证的一组任务。达到工具或上下文限制时，保存代码和准确交接信息，标明下一步，不要把部分完成说成全部完成。

现在先简短说明环境检查结论和阶段一的验收条件，然后开始修改项目文件。
```

## 附录 B：适合分轮开发的六段提示词

这些提示词用于限制单轮任务范围。每次提供当前代码、完整需求文档和交接文档，然后只发送当前阶段对应的一段。

### B1：第一轮——工程与视觉基线

```text
本轮仅执行完整需求文档的阶段 1。

重点阅读：第一至四节、第七至八节、阶段 1、十三至十四节。其他功能作为后续背景，不在本轮铺开。

检查或初始化工程，建立设计 Token、响应式应用框架、路由和 IndexedDB。完成真实可持久化的任务新增、编辑、完成、重新打开、软删除及基本筛选，做出有完成度的总览核心区域。

严格落实深空底色、青色焦点、清晰留白和克制的科技感。页面内容使用真实任务数据；没有数据时提供可操作空状态。

验证创建任务后刷新仍存在，以及完成/重新打开后的状态持久化。运行类型检查和构建。更新三份项目文档，列出实际结果与阶段 2 的入口。

本轮停止在阶段 1 验收结束，避免分散到尚未实现的领域功能。
```

### B2：第二轮——目标、项目、任务和习惯

```text
继续当前 StarDesk 工程，仅执行完整需求文档的阶段 2。

先阅读 docs/PRODUCT_SPEC.md、docs/TASKS.md、docs/HANDOFF.md 和现有数据模型，确认阶段 1 的任务持久化闭环仍然工作。

基于现有架构完成目标、项目、关联任务、子任务、今日计划、最多三项重点、习惯计划及打卡、简单日程。统一目标进度和日期计算，尤其处理任务去重、子任务计权、无有效任务、休息日和过去频率规则。

不重建主题或数据库，不复制另一套任务状态。数据库结构变化使用增量迁移。

用一个目标、一个项目、多项任务和一个每周计划习惯验证完整流程。检查跨天、周边界和同日重复打卡。

更新文档与检查结果，本轮停止在阶段 2 验收结束。
```

### B3：第三轮——四个领域和知识库

```text
继续当前 StarDesk 工程，仅执行完整需求文档的阶段 3。

先阅读交接文档和当前通用实体、仓储与组件。重点实现个人提升、职业规划、兴趣爱好、运动健身四个领域，以及统一知识库。

四个领域共用目标、项目、任务、笔记和资源；按需求补充学习、兴趣和运动记录、职业技能自评、可选体重记录。各领域要有清晰的实际用途和真实数据摘要。

不要为领域复制任务表，不要仅修改标题和颜色后交付四张相同空页面。按照完整需求中第五节的领域验收示例逐一走通。

把领域数据接入首页；检查任务完成与实际投入时长的区别，避免重复统计。

运行针对本轮变更的检查，更新文档，本轮停止在阶段 3 验收结束。
```

### B4：第四轮——搜索、专注与复盘

```text
继续当前 StarDesk 工程，仅执行完整需求文档的阶段 4。

先确认已有领域与知识库正常。实现全局搜索、带上下文的快速新增、专注计时，以及周/月复盘。

重点处理：搜索打开真实详情；专注基于时间戳而非计数器；刷新和后台恢复；重复结束防重；专注转换为领域记录时防重；复盘保存统计快照；复盘行动只创建一次。

统一趋势图、统计单位和无数据状态。没有连接模型 API 就不显示可用的 AI 分析入口。

运行对应业务测试及关键交互检查，更新文档，本轮停止在阶段 4 验收结束。
```

### B5：第五轮——扩展模块与备份恢复

```text
继续当前 StarDesk 工程，仅执行完整需求文档的阶段 5。

完成统一模块注册表、普通自定义模块、模块显示配置、首页组件配置、数据导出与完整替换恢复、回收站、必要迁移，以及正式/演示空间隔离。

务必区分代码里的模块定义和数据库里的模块配置。新增普通模块只能组合已有能力，不能声称自动生成任意业务逻辑。

备份包含所有业务数据、关系和配置。导入先完整校验，再预览并确认，最后事务写入；任何错误都保留原数据。

在隔离测试空间走通导出与恢复，比较实体数量、关系、习惯历史、设置和统计。测试损坏文件、重复 ID、缺失引用和不支持的版本。

验证创建“旅行计划”模块后无需改代码即可使用通用功能，改名和停用不会丢失数据。

更新文档，本轮停止在阶段 5 验收结束。
```

### B6：第六轮——体验检查与完整交付

```text
继续当前 StarDesk 工程，仅执行完整需求文档的阶段 6。

先根据 TASKS 和现有实现列出差距，修复必须完成的缺项，再进行视觉与体验打磨。

检查 360、390、768、1024、1440 和 1920px 的布局，检查深浅主题、键盘操作、弹窗焦点、长内容、空状态、加载和错误状态、减少动态效果。

运行完整需求文档第十三节的关键流程和实际工程检查。对已有数据有影响的修改必须保留迁移与恢复能力。

使用当前运行页面截图说明视觉结果。没有截图工具或浏览器时明确标注未验证，不生成假的测试证据。

交付可运行应用、准确 README、最新交接文档，并区分已验证、待验证、未实现三类内容。
```

## 附录 C：换模型、换对话时的接手提示词

```text
你现在接手的是一个已经开始开发的项目，不是新建项目。

请先阅读：
1. 完整需求文档。
2. docs/PRODUCT_SPEC.md。
3. docs/TASKS.md。
4. docs/HANDOFF.md。
5. package.json、锁文件、路由、数据库定义与当前阶段相关源码。

以实际代码和运行结果确认当前状态；文档与代码不一致时，先查明原因并修正文档。不要仅根据文件存在就认为功能完成。

保持已确认的产品范围、视觉语言、技术栈、数据模型和持久化方式。不要重新创建工程，不要清空数据库，不要为图方便更换框架或用静态内容替换功能。

根据 HANDOFF 的下一步任务，选择一个能在本轮完成并验证的小范围继续开发。必要的结构调整先说明原因和影响，然后在现有项目中增量完成。

结束前更新 TASKS 与 HANDOFF，写出改动、实际检查结果、未完成事项及下一轮入口。
```

## 附录 D：后续新增模块提示词

将方括号中的内容替换成自己的需求：

```text
请在现有 StarDesk 工作台上新增模块：

模块名称：[例如：旅行计划]
希望解决的问题：[例如：整理旅行准备事项、目的地资料和行程记录]
我最需要的三个功能：
1. [功能一]
2. [功能二]
3. [功能三]

先阅读当前模块注册表、通用实体、数据访问层、导航、搜索、首页摘要、备份格式和交接文档。

判断这项需求是否能通过普通模块的现有能力实现。可以的话优先创建配置和模板；只有需要专属行为时，才增加新的功能模块和必要实体。

保持既有 UI Token 和交互方式，复用已有目标、项目、任务、笔记和记录，不复制一套相同数据。

新增持久化结构必须提供校验、增量迁移和导入导出适配。模块改名、隐藏、归档和恢复都要保持关联可靠。

实现后验证新模块的完整核心流程，并验证原有模块仍可使用，更新 PRODUCT_SPEC、TASKS、HANDOFF 和必要 README。
```

## 附录 E：专门修复问题的提示词

```text
请修复当前 StarDesk 项目的以下问题：
[粘贴错误现象、触发步骤、报错或截图说明]

先复现并定位原因，检查相关业务规则和现有实现。保持其余功能、视觉和数据不变，不要通过删除功能、弱化类型检查、清空数据库或重写整个应用绕开问题。

用最小但完整的改动修复；涉及日期、统计、关联、导入导出等数据逻辑时，补充能防止该错误再次发生的必要测试。

验证原问题消失，并检查相邻关键流程。最后说明根因、改动、实际验证结果与仍未验证的部分，更新交接文档。
```

## 附录 F：专项 UI 打磨提示词

```text
请对现有 StarDesk 做一次有边界的 UI 打磨，保留业务逻辑、数据模型、字段和已有功能。

按照完整需求第四节统一深空底色、青色焦点、字体层级、边框、留白、卡片、图表、弹窗和交互状态。科技感通过精细布局和真实数据表达，不堆叠装饰特效。

优先打磨：首页的今日重点、四个领域卡片、行动中心、任务详情、快速新增和手机布局。

检查深浅主题、空状态、长标题、表单错误、键盘焦点和减少动效。发现数据功能不完整时记录具体问题，不用漂亮静态页面替代原功能。

完成后用实际运行截图对照说明改进，报告验证过的屏幕宽度，更新交接文档。
```


